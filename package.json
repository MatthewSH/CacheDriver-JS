{
  "name": "cache-driver",
  "version": "1.0.0",
  "description": "A cache driver interface that can be implmented into any package that needs the ability to support multiple caching systems.",
  "keywords": ["cache", "driver", "storage", "caching", "find", "get", "save", "file cache", "file", "json cache", "simple cache"],
  "main": "lib/Cache.js",
  "types": "lib/Cache.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "./node_modules/.bin/tsc",
    "prepublish": "npm run compile"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MatthewSH/CacheDriver-JS.git"
  },
  "author": "matthewh",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/MatthewSH/CacheDriver-JS/issues"
  },
  "homepage": "https://github.com/MatthewSH/CacheDriver-JS#readme",
  "files": [
    "/lib",
    "LICENSE",
    "README.md"
  ],
  "devDependencies": {
    "@types/codependency": "^0.1.3",
    "typescript": "^3.2.4"
  },
  "dependencies": {
    "codependency": "^2.1.0"
  },
  "optionalPeerDependencies": {
    "cache": "^2.1.0"
  }
}
